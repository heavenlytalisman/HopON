rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default deny - HopON uses Cloudinary for storage
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
